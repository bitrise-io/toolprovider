---
format_version: "17"


workflows: {}

meta:
  bitrise.io:
    stack: osx-xcode-15.0.x
  experimental:
    tools:
      golang: 1.16.3
      nodejs: 20:installed
      ruby: 3.2:latest
      flutter:
        version: 3.32.5-stable
        plugin: flutter::https://github.com/asdf-community/asdf-flutter.git
      python: "3.13:installed"
      tuist:
        version: ":latest"  # Should install latest released.
      alias:
        version: "latest"  # Should install latest released.
        plugin: alias::https://github.com/andrewthauer/asdf-alias.git
      signal-cli: {}  # Should install latest released.
      air:
        version: "installed"  # Should install latest installed.
        plugin: air::https://github.com/pdemagny/asdf-air
      elixir:
        version: ":installed"  # Should install latest installed.
