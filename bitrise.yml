format_version: "17"


workflows: {}

meta:
  bitrise.io:
    stack: "osx-xcode-15.0.x"
  experimental:
    tools:
      golang:
        version: "1.16.3"
      nodejs:
        version: "20"
        resolution_strategy: closest_installed
      ruby:
        version: "3.3"
        resolution_strategy: closest_installed
